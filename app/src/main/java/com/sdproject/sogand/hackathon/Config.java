package com.sdproject.sogand.hackathon;

/**
 * Backend endpoint configuration.
 *
 * The original hackathon build pointed at a hardcoded EC2 instance, which has
 * long since been torn down. Set BASE_URL to your own server (no trailing
 * slash) to run the app against a live backend -- see the README for the
 * routes it expects.
 */
public class Config {

    /** e.g. "http://10.0.2.2:5050" for the Flask backend on an emulator host. */
    public static final String BASE_URL = "";

}
